# 变美计划工作台 · 全部指令汇总（2026-07-27 ~ 2026-08-03）

> 来源：各会话 `.workbuddy/memory` 工作日志（共 11 个文件）。  
> 项目文件目录：`C:\Users\张滢璐\WorkBuddy\2026-07-27-16-14-48`  
> 说明：以下每条是你当时下的**指令/需求**（括号内为实现要点，供对照）。按时间顺序排列。

---

## 2026-07-27 · 工作台创建与风格迭代

1. 搭建纯静态个人工作台（index.html + styles.css + app.js，无 CDN、可离线）；左侧固定导航 6 菜单（年历/个人分析/资源库/日计划/周计划/100天总结）；数据存 localStorage，支持 JSON 导入导出。
2. 视觉升级为**古典欧式/巴洛克**（木纹描金侧栏、象牙白描金面板、衬线字体、巴洛克 SVG 花纹框）。
3. 风格升级为**欧式宫廷风**（凡尔赛式宝蓝 + 描金）。
4. 风格切换为**洛可可宫廷风**（粉金玫瑰、奶白墙 + 描金卷草 + 玫瑰花纹）。
5. 天使油画作为**整张工作台背景**（参考图：天使在描金画框中、环绕玫瑰）。
6. 把参考图**裁剪到宽屏比例**（16:9）。
7. 修复「图片只显示一半」→ 改为**完整呈现**（contain）。
8. 切换为**横版天使油画、完全铺满**（1280×719 放大到 1920×1080）。
9. 侧栏图标改为**欧式描金 SVG**（6 个手绘线条图标替换 emoji）。
10. 年历新增**100 天打卡区间**灰底标记（选起始日 → 自动算 100 天灰底纹）。

## 2026-07-28 · 个人分析面板

1. 年历新增**法定节假日标注**（放假日打 `*`，首日标节日名）。
2. 个人分析页改为**个人数据面板**（左图右表：上传全身照 + 自动算头身比/头肩比 + 录入身高体重三围等，保存后下次自动加载）。
3. BMI **数值自动分析**（偏瘦/正常/偏胖）。
4. 删除个人分析页**底部导航**（目录/年历/资源库）。
5. 个人分析新增**面部分析区块六~十**（骨相/比例/五官/皮相/动态，33 个单选项 + 数字输入）。
6. 个人分析新增**三张面部照片上传区**（正面 / 侧面45° / 纯侧面）。
7. 个人分析分两大部并标注（区块六起标「小框架分析」，顶部「大框架分析」）。

## 2026-07-29 · 正面分析

1. 删除面部分析区块六~十，连带删除**面部三视角照片上传区**。
2. 新增区块六\*\*「正面分析」\*\*（上传正面照 → 自动标关键点 → 拖拽校准 → 确认分析算指标）。
3. 强化区块六**指标展示**（指标显示在区块内，加标题 + 卡片样式）。

## 2026-07-30 · 面部细节 + 日计划 + 周计划

1. 区块六脸型字段改为**多选**（圆/方/短/长，最多 2 项）。
2. 脸型多选**布局换排**（短/长换到第二排）。
3. 眼型区新增**眉眼间距**单选。
4. 眼距下新增**鼻翼宽度**单选。
5. 鼻翼宽度下新增**人中长度**单选。
6. 将眉眼间距/鼻翼宽度/人中长度**接入照片自动分析**。
7. 区块六扩展：**三庭五眼辅助线 / 对称性热力图 / 标点校准 / 单项细查 / 历史对比**。
8. 取消「**历史对比**」功能。
9. 取消「**单项细查**」功能。
10. 修复资源库「新增资源」弹窗**取消点不了**（全局弹窗 `hidden` bug）。
11. 重建**日计划**页面：四大功能（任务打卡/习惯追踪/时间规划/复盘对比）+ 三栏布局。
12. 日计划「跟练」**接入资源库内容**（从资源库添加跟练视频）。
13. 睡眠改为**八个月亮可点选**。
14. 跟练开关改为**每项独立**（每资源后放完成开关）。
15. 时间轴改为**24 圆点刻度**（第 8、16 个实心黑点）。
16. 习惯打卡新增**自定义习惯**可加项。
17. 时间轴**三段时段备忘输入框**（00-08 / 08-16 / 16-24）。
18. 三段输入框改为**时间轴旁边**（非底部）。
19. 对比图**加入照片添加**能力。
20. 对比图**缩略图缩小**。
21. 对比图**删除输入框 + 放大照片**。
22. 对比图最底部新增**用户备注输入框**。
23. 修复资源库**删除不了**（原生 confirm 被拦截 → 自定义弹窗）。
24. **周计划页面重构**（本周重点 + 完成奖励 + 7 天打卡格 + 上周/下周切换）。
25. 生成**区块式页面模板文件**（block-page-template.html）。
26. 周计划新增区块三**数据对比**（体重/腰围/体脂率，自动算差值 + 趋势箭头）。
27. 周计划数据对比**上方加图片选项**（上周/本周照片）。

## 2026-07-31（上午）· 周计划跳转与输入框

1. 周计划打卡格**跳转日计划**（每格加 ↗ 按钮）。
2. 修正：改为**统一「打开日计划」按键**（非每格按钮）。
3. 日计划顶部加\*\*「打开周计划」跳转\*\*（对称双向）。
4. **跳转按钮美化**（胶囊图标按钮）。
5. 周计划每日打卡**加入输入框**（每天安排备注）。
6. 打卡**输入框加大**。
7. 打卡**输入框再加大**。
8. 打卡格子内**加跳转日计划**（↗ 小圆标）。

## 2026-07-31（下午~晚）· 变美计划品牌 + 100天总结 + 情绪盒子 + 宠物之家

1. 周计划「本周重点」改为**可动态添加/删除**目标列表。
2. 周计划快捷标签改为：早睡/控糖/睡眠/运动/体态/化妆/护肤/穿搭/减肥。
3. 左侧导航品牌区重做：标题「个人工作台」→**「变美计划」**，logo 改描金五瓣玫瑰。
4. 「变美计划」标题由描金渐变改为**棕色**。
5. logo 改**小天使 cherub**（后重做干净版，再改古典 putto，翅膀再大一点）。
6. 日计划对比图底部备注上方新增\*\*「数据对比」区块\*\*（前/后指标）。
7. **100 天总结页面** 11 区块整体实现。
8. 日计划习惯打卡新增\*\*「心情」\*\*（8 笑脸 → 改 7 个）。
9. 100 天总结**计划起点联动年历**打卡起点。
10. 100 天总结**区块二重构**（9 项身体数据）。
11. 修复点开 100 天总结区块二及之后**不显示**（deepMerge）。
12. 100 天总结横幅/区块样式系列调整：
    - 顶部庆祝横幅换**奖章模板** → 去 emoji → 对齐日计划区块 → 加浅色外框 → 取消区块六「本周重点汇总」 → 横幅改套用区块模板 → 去区块 2 前缀 → 区块 2 改用户增删 → 去区块 3-10 前缀 → 区块 2 删身高/头身比/头肩比 → 区块 5 按周排列 → 完成状态依据周计划 → 起点日即 Day1 → 完成格绿勾 → 删「面部变化对比」区块 → 完成奖励可添加 → 调整区块顺序。
13. 新增\*\*「宠物之家」养成模块\*\*（宠物数据/成长阶段/悬浮窗/宠物之家页面）。
14. 宠物之家入口做成**底部导航条**。
15. **删除底部导航条**，恢复左侧导航。
16. 实现\*\*「情绪盒子」治愈盲盒\*\*模块（盲盒+开盒动画+随机话语+每日提醒+年历联动+话语管理）：
    - 盲盒网格改两排 3 列 → 情绪图标改手绘 SVG 脸 → 升级为小猫造型 → 旧数据无 icon 兜底 → 开盒加小猫探头动画 → 重画圆润猫脸 → 重做毛茸茸 Q 版小猫（解决像老鼠） → 多次「没更新」根因是预览路径缓存（脚本改名 app.sticker.js） → 改**卡通贴纸风**。

## 2026-08-01 · 资源库 + 100天总结精简

1. 资源库：**新增资源置顶**（unshift 到列表最前）。
2. 资源库：**资源列表移至金字塔上方**（页面布局位置）。
3. 100 天总结：删除「**数据导出与分享**」区块。
4. 100 天总结：删除「**用户自我反思记录**」区块。

## 2026-08-02（上午~下午）· 侧面分析 + 三餐 + 食物识别 + 经期

1. 新增「**七、侧面分析**」区块（侧面照 + 13 关键点 + 自动算侧面型）。
2. 删除 4 个角度指标（鼻额角/鼻唇角/颏颈角/面凸角）。
3. 区块七「侧面分析」唇形下新增\*\*「下颌线」单选\*\*。
4. 正面分析新增**可拖动九宫格参考线**（三分法网格）。
5. 时间轴**备忘录字号放大**。
6. 日计划新增「**一日三餐**」营养跟踪区块（目标 + 早午晚卡片 + 自动算热量/宏量）。
7. **拍照识别食物**（本地 TensorFlow.js + MobileNet，不联网无 API）。
8. 年历新增「**姨妈期（经期）**」跟踪（标记起始 + 预测 + 提醒 + 自动算周期）。

## 2026-08-02（晚）· 宠物换版

1. 删除宠物之家（后改口：**宠物换第一版**）。
2. 宠物换成**第一版橘猫「团子」**（pet.js）。

## 2026-08-03 · 经期功能反复打磨与缓存修复

1. 日计划「一日三餐」卡片**溢出边框修复**。
2. 年历「生理期」与「100 天打卡区间」**视觉分离**（三通道不覆盖）。
3. 年历「生理期」与「100 天打卡区间」**拆分为两个独立框**。
4. **取消两个框** → 恢复单张年历（三层视觉通道）。
5. 经期标记改为\*\*「点两次成区间、可重复多次」\*\*。
6. 修复经期清除**点了没反应**（原生 confirm 拦截 → confirmDialog）。
7. 经期清除仍没反应 → **换端口 8195** 强制加载。
8. 经期预测文案新增\*\*「间隔 X 天」显示\*\*。
9. 经期设置新增\*\*「间隔天数」可自动设置\*\*（建议值一键填入）。
10. **根因定位**：预览内核缺 `structuredClone` → 改用 `clone()` 兼容。
11. 加**版本标 wbVerTag** + 换端口 8210。
12. 经期设置新增\*\*「默认周期」手动添加经期\*\*。
13. **删掉经期设置和清除按键**（移除整块经期设置功能，保留核心标记/预测/提醒）。
14. **清除按键也删掉**（移除年历「清除」按钮 calClear）。
15. 「预计下次经期」间隔改为**用户手动输入**（年历内直接输间隔天数）。

---

### 统计

- 跨期：2026-07-27 → 2026-08-03（约 8 天）·
- 指令总数：约 **100 条**
- 高频主题：视觉风格迭代（巴洛克→宫廷→洛可可→天使油画）、个人/面部分析、日计划、周计划、100 天总结、情绪盒子、宠物之家、经期跟踪、预览缓存问题反复出现。
